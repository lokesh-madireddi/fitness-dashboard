apiVersion: v1
kind: Service
metadata:
  name: fitness-dashboard-service
spec:
  type: LoadBalancer
  selector:
    app: fitness-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
